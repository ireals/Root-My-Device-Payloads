{
  "$comment": [
    "Every supported target, keyed by device, region and kernel release. The",
    "files for an entry live at",
    "src/targets/<device>/<region lowercased>/<kernelRelease>/ -- the path is",
    "derived, never written here, so the two cannot drift apart.",
    "",
    "'core' names the exploit tree under src/payloads/<payload>/ that this",
    "target's kernel needs, and with it the target-<core>.h in the directory",
    "above and the root-<core>.c glue. The attack chain is fixed to a GKI",
    "branch rather than to a SoC, so a kernel on a different series takes a",
    "different core -- not the same core with different offsets.",
    "",
    "This is the hand-authored half of the support feed. Artifact URLs and",
    "sizes are not here: they are only knowable after CI builds them, and",
    "tools/generate_feed.py joins the two into the targets-v2.json the app",
    "reads. See the repository README.",
    "",
    "kernelVersion is the complete /proc/version line and kernelBuildVersion is",
    "the '#<n> SMP ...' part of it. `adb shell cat /proc/version` on the device",
    "is the direct way to get them; both are also the kernel's own linux_banner",
    "and UTS_VERSION, so they can be read out of the boot image when no device",
    "is at hand -- that only proves what the image would print, not what the",
    "slot the device actually boots does. A target whose kernelVersion is null",
    "is carried here but left out of the feed, because the app matches on those",
    "exact strings."
  ],
  "schemaVersion": 1,
  "targets": [
    {
      "profileId": "pmg110-cn-16.0.9.400",
      "payload": "CVE-2026-43499",
      "core": "core66",
      "device": "pmg110",
      "region": "CN",
      "manufacturer": "OPPO",
      "model": "PMG110",
      "marketingName": "OPPO K15 Pro+",
      "soc": "MediaTek MT6991",
      "kernelRelease": "6.6.118-android15-8-g93e223c276e7-abogki500782043-4k",
      "kernelVersion": "Linux version 6.6.118-android15-8-g93e223c276e7-abogki500782043-4k (kleaf@build-host) (Android (11368308, +pgo, +bolt, +lto, +mlgo, based on r510928) clang version 18.0.0 (https://android.googlesource.com/toolchain/llvm-project 477610d4d0d988e69dbc3fae4fe86bff3f07f2b5), LLD 18.0.0) #1 SMP PREEMPT Wed Apr  8 15:08:30 UTC 2026",
      "kernelBuildVersion": "#1 SMP PREEMPT Wed Apr  8 15:08:30 UTC 2026",
      "buildDisplay": "PMG110_16.0.9.400(CN01)",
      "buildFingerprint": "OPPO/PMG110/OP61E5L1:16/BP2A.250605.015/B.c24acd_188efc3_187038b:user/release-keys",
      "sdk": 36,
      "abi": "arm64-v8a",
      "pageSize": 4096,
      "status": "the exploit chain is device-verified here through pmg110-root, the tree core66 was imported from; the payload this feed serves has not completed a run, and no build of it could have reported one until the root glue was wired up -- core66's run_exploit() reaches root in a forked child and then called a weak ENOSYS stub, so neither 'exploit completed' nor 'done=1 root=1' could ever be printed. kernelVersion / kernelBuildVersion are read out of boot.img rather than captured from the device"
    },
    {
      "profileId": "warhol-jp-OS3.0.304.0.WPSJPXM",
      "payload": "CVE-2026-43499",
      "core": "core612",
      "device": "warhol",
      "region": "JP",
      "manufacturer": "Xiaomi",
      "model": "Xiaomi 17T Pro",
      "marketingName": "Xiaomi 17T Pro",
      "soc": "MediaTek MT6993",
      "kernelRelease": "6.12.38-android16-5-g1d46253471dd-ab15048002-4k",
      "kernelVersion": "Linux version 6.12.38-android16-5-g1d46253471dd-ab15048002-4k (kleaf@build-host) (Android (14043575, +pgo, +bolt, +lto, +mlgo, based on r536225) clang version 19.0.1 (https://android.googlesource.com/toolchain/llvm-project b3a530ec6537146650e42be89f1089e9a3588460), LLD 19.0.1) #1 SMP PREEMPT Wed Mar 18 02:21:10 UTC 2026",
      "kernelBuildVersion": "#1 SMP PREEMPT Wed Mar 18 02:21:10 UTC 2026",
      "buildDisplay": "BP2A.250605.031.A3",
      "buildFingerprint": "Xiaomi/warhol_jp/warhol:16/BP2A.250605.031.A3/OS3.0.304.0.WPSJPXM:user/release-keys",
      "sdk": 36,
      "abi": "arm64-v8a",
      "pageSize": 4096,
      "status": "the LD_PRELOAD build roots this device on the first attempt, and every matched field here -- kernelRelease, kernelVersion, kernelBuildVersion, buildFingerprint, buildDisplay, sdk, abi, pageSize -- is read off the device rather than reasoned from the image; `getprop ro.build.display.id` returns the fingerprint's ID field, not the incremental this entry first guessed. The application route is complete as of the init-spawned bootstrap helper: run from the app on release payloads-3-708b215 it reached done=1 root=1 on the first attempt at uid 10340, and ksud then loaded KernelSU -- `kernelsu.ko loaded successfully`, `KernelSU live: late_load=true` -- the version that run printed was 30001, which was the build's shallow submodule and not the module; rebuilt from full history it reads 32525 and the manager accepts it, shown as `32525-2` on its home screen -- with system_server at its original pid and all three Zygisk modules in place. The helper is exec'd by init, not forked from the app, because an app process carries a seccomp filter that no amount of uid 0 removes"
    },
    {
      "profileId": "klimt-jp-OS3.0.301.0.WOSJPXM",
      "payload": "CVE-2026-43499",
      "core": "core66-2",
      "device": "klimt",
      "region": "JP",
      "manufacturer": "Xiaomi",
      "model": "Xiaomi 15T Pro",
      "marketingName": "Xiaomi 15T Pro",
      "soc": "MediaTek MT6989",
      "kernelRelease": "6.6.89-android15-8-g0889fe95bb10-ab14402178-4k",
      "kernelVersion": "Linux version 6.6.89-android15-8-g0889fe95bb10-ab14402178-4k (kleaf@build-host) (Android (11368308, +pgo, +bolt, +lto, +mlgo, based on r510928) clang version 18.0.0 (https://android.googlesource.com/toolchain/llvm-project 477610d4d0d988e69dbc3fae4fe86bff3f07f2b5), LLD 18.0.0) #1 SMP PREEMPT Fri Nov  7 02:16:40 UTC 2025",
      "kernelBuildVersion": "#1 SMP PREEMPT Fri Nov  7 02:16:40 UTC 2025",
      "buildDisplay": "BP2A.250605.031.A3",
      "buildFingerprint": "Xiaomi/klimt/klimt:16/BP2A.250605.031.A3/OS3.0.301.0.WOSJPXM:user/release-keys",
      "sdk": 36,
      "abi": "arm64-v8a",
      "pageSize": 4096,
      "status": "the adb route (LD_PRELOAD, non-app build) roots this device; the exploit chain reached uid 0 and SELinux permissive through the standalone klimt-BP2A.250605.031.A3 build. The offsets in target-core66.h are from IDA MCP on this firmware's vmlinux (SHA256 14c19e6ae18d0ce7db695e336292ae2c19a67902739d5cc8a2b78b10914010c8). kernelVersion and kernelBuildVersion are from /proc/version on the device. The core66 payload build has not been tested on this device"
    },
    {
      "profileId": "xig07-jp-OS3.0.7.0.WNEJPKD",
      "payload": "CVE-2026-43499",
      "core": "core61",
      "device": "xig07",
      "region": "JP",
      "manufacturer": "Xiaomi",
      "model": "XIG07",
      "marketingName": "Xiaomi 14T (au XIG07)",
      "soc": "MediaTek MT6897",
      "kernelRelease": "6.1.138-android14-11-g44bda9e8f6e9-ab13792638",
      "kernelVersion": "Linux version 6.1.138-android14-11-g44bda9e8f6e9-ab13792638 (build-user@build-host) (Android (10087095, +pgo, +bolt, +lto, -mlgo, based on r487747c) clang version 17.0.2 (https://android.googlesource.com/toolchain/llvm-project d9f89f4d16663d5012e5c09495f3b30ece3d2362), LLD 17.0.2) #1 SMP PREEMPT Wed Jul 16 16:56:31 UTC 2025",
      "kernelBuildVersion": "#1 SMP PREEMPT Wed Jul 16 16:56:31 UTC 2025",
      "buildDisplay": "BP2A.250605.031.A3",
      "buildFingerprint": "Xiaomi/XIG07_jp_kdi/XIG07:16/BP2A.250605.031.A3/OS3.0.7.0.WNEJPKD:user/release-keys",
      "sdk": 36,
      "abi": "arm64-v8a",
      "pageSize": 4096,
      "status": "the first android14-6.1 target, and the first to name core61. The adb route (LD_PRELOAD, non-app build) roots this device end to end on the first attempt in 5 runs out of 7, each started from a boot with at least 9 minutes of uptime (two of the seven were second runs on a boot an earlier run had left alive; counting only first runs it is 4 of 5) -- the other 2 panicked in the sched_setattr punch before the R/W, so a miss costs a ~90 s reboot rather than one of the supervisor's remaining attempts, and the run after one that rooted fails on the per-uid pipe budget its resident helpers still hold: `root install done=1 root=1 uid=2000->2000 su daemon serving` -- the exploit process stays unprivileged on this route by design, that pair is its own uid and `root=1` is the flag saying the helper the kernel exec'd reached uid 0, which the summary line prints as `uid=2000->0` -- and the daemon answers `uid=0(root) ... context=u:r:kernel:s0` -- not the KernelSU that is in this device's init_boot as a debugging aid, which answers u:r:ksu:s0. Every matched field here except kernelVersion is read off the device rather than reasoned from the image, and the offsets come from that device's own boot.img; the bug is unfixed in this image and the waiter shape is the pre-split 0x58 one, both re-checked in the disassembly rather than inherited from the core's other 6.1 targets. Everything the application does after it has a payload is verified too, driven from adb: the release .so loaded through the helper's own --run-payload runner reports both markers the app requires (`exploit completed`, `done=1 root=1`) on the first attempt from a clean boot, the helper stages ksud and `--late-load android14-6.1 me.weishu.kernelsu` answers `KernelSU loaded and answering (0)`, and the manager then shows the LKM Jailbreak-mode card at version 32525-2 with SELinux back to Enforcing, the framework never restarting under any of it (system_server still the pid it took 9.6 s into boot, sixteen minutes and one late-load later) -- this is also the first android14-6.1 KernelSU build in the repository to be loaded anywhere. Only the app's two log markers are checked that way, not the runner exit status its installer also gates on, because the runner is backgrounded so the log can be streamed off before a panic. The application's own route works too, as of core61 leaking the slide through boot_id: two runs from the app on two clean Enforcing boots, first attempt each, uid 10325 in u:r:untrusted_app:s0, reached `done=1 root=1` and the manager then showed the LKM Jailbreak-mode card at 32525-2 with SELinux back to Enforcing and system_server still the pid it took 9.4 s into boot. tracefs is still EACCES from that domain (`slide tracefs setup failed errno=13`); what changed is that it is no longer fatal -- pr_error's exit(-1) was, and the run now falls through to the boot-id source. Two runs are not a rate: the slide adds a second grooming-and-punch stage to a chain whose stages land about two in three, and the app caches the slide per boot so a retry skips that stage. kernelVersion is set from this device's own /proc/version, which puts the target in the feed and its KernelSU build in the job a release waits on. What that has *not* been through is the feed path itself: every run so far read its payload from a folder on the device through the application's debug mode, so the release tag, the asset URLs anchored to it and the size checks will run for the first time when CI publishes this target, as will CI's own build of the app payload and of the android14-6.1 KernelSU pair, which has only ever been built locally. The first feed-served run is the thing to watch The temporary root itself is verified; note that a run leaves SELinux permissive until the next reboot, since nothing on this route puts it back"
    }
  ]
}
